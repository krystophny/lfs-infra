# LFS Build Configuration
# Copy to .env and fill in values: cp .env.example .env
# .env is gitignored, .env.example is not

# Build Configuration
LFS_BUILD_DIR=/var/tmp/ert

# USB System User (default: ert, random password if empty)
LFS_USERNAME=ert
LFS_PASSWORD=

# WiFi (optional - configure after boot if empty)
WIFI_SSID=
WIFI_PASSWORD=

# LAN: 1=DHCP (default), 2=Static
LAN_MODE=1

# Static IP settings (only if LAN_MODE=2)
LAN_IP=
LAN_GATEWAY=
LAN_DNS=1.1.1.1
