# LFS Installation Configuration
# Copy to .env and edit: cp .env.example .env
# .env is gitignored for security

# Target device (required for unattended install)
# LFS_DEVICE=/dev/nvme0n1

# Mount point during build
LFS_MOUNT=/mnt/lfs


# User account
LFS_USERNAME=
LFS_PASSWORD=

# System configuration
LFS_HOSTNAME=lfs
LFS_TIMEZONE=

# Kernel configuration (optional)
# LFS_KERNEL_CONFIG=/path/to/custom.config

# WiFi configuration (optional)
# WIFI_SSID=MyNetwork
# WIFI_PASSWORD=secret

# Static LAN configuration (optional)
# LAN_INTERFACE=eth0
# LAN_IP=192.168.1.100
# LAN_NETMASK=255.255.255.0
# LAN_GATEWAY=192.168.1.1
# LAN_DNS=1.1.1.1
