# LFS Build Configuration Example
# Copy to .env and fill in your values: cp .env.example .env
# This file is NOT gitignored - .env IS gitignored

# USB System User
LFS_USERNAME=lfs
LFS_PASSWORD=yourpassword

# WiFi Configuration (leave empty to skip WiFi)
WIFI_SSID="Your WiFi Name"
WIFI_PASSWORD="yourwifipassword"

# LAN Configuration
# LAN_MODE: 1=DHCP, 2=Static
LAN_MODE=1

# Static IP settings (only used if LAN_MODE=2)
# Format: IP/CIDR (e.g., 192.168.1.100/24)
LAN_IP=
LAN_GATEWAY=
LAN_DNS=
